import 'package:flutter/material.dart';

class WaterMonthView extends StatelessWidget {
  const WaterMonthView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("inside water month view");
  }
}
