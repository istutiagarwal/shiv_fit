import 'package:flutter/material.dart';

class WaterWeekView extends StatelessWidget {
  const WaterWeekView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("inside water week view");
  }
}
