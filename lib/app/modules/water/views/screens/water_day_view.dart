import 'package:flutter/material.dart';

class WaterDayView extends StatelessWidget {
  const WaterDayView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("inside water day view");
  }
}
